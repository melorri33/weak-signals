# Контекст из чата с Claude (18.09.2026)

Выжимка того, что обсудили и решили до создания репозитория. Для агента Claude Code: прочитай этот файл вместе с `CLAUDE.md`, чтобы продолжить с того же места.

## Задача (кейс Газпромбанк.Тех)

- ТЗ: «Сервис для автоматизированного сбора и анализа зарождающихся трендов (слабые сигналы) в научно-технологических отраслях», 10 страниц. Файл ТЗ проверен на скрытые инструкции для ИИ (белый/мелкий/невидимый текст, слои, вложения, скрипты, картинки) — чисто.
- Этап 1: модель на выданном датасете, точность 75–80%+, отчёт Precision/Recall/F1, интерпретируемость.
- Этап 2: открытый запрос → поиск по открытым источникам в реальном времени → топ-15 слабых сигналов с описанием, преимуществом, кейсом, источниками, скорингом и объяснением; отсев зрелых трендов, хайпа и шума.
- Все требования ТЗ, которые нельзя нарушать, — в `CLAUDE.md`, раздел «Жёсткие требования ТЗ».
- Оценка: подход и методология; качество кода и развёртывание; интерпретируемость и отсев шума; точность на закрытом датасете + экспертная оценка жюри; питч (финал).

## Решения команды

- Команда — 4 человека, код пишут с помощью Claude.
- **artiom — ML + ядро.** Репозиторий на его аккаунте: он мёржит в `main` и один меняет `src/common/schemas.py`.
- Роли остальных: Данные, Интеграция, Продукт (файлы в `.claude/agents/`).
- LLM — **локальная**: Qwen через Ollama. Облачный API из списка ТЗ — только как запасной и после согласования.
- Интерфейс — сначала Streamlit; React — если останется время после промежуточной сдачи (решает команда).
- Промежуточная сдача — через 10 дней, ≈ 28.09.2026. Дата финала неизвестна.

## Сложность ролей (для распределения людей)

1. **ML + ядро** (artiom) — очень высокая: метрика на незнакомом датасете, переносимые признаки, SHAP, ревью всех PR.
2. **Интеграция** — высокая: локальная LLM, выделение кандидатов, проверка ссылок, конвейер с бюджетом времени, Docker; ловит ошибки на стыках. → самому сильному в коде.
3. **Данные** — средняя: много API, лимиты, форматы, дубли, кэш. → аккуратному и методичному.
4. **Продукт** — низкая по коду, высокая по объёму и влиянию на субъективную оценку. → тому, кто лучше придумывает и выступает.

Предложено, **пока не внесено в файлы ролей**:

- Данные после дня 6 забирают у Интеграции Docker и README.
- Продукт пишет тексты промптов (`src/llm/prompts/`) вместе с Интеграцией.
- artiom внимательно ревьюит конвейер, контракты и ML; PR по UI и документам — быстрым просмотром.

## План до промежуточной сдачи (сжато)

| Дни | ML | Данные | Интеграция | Продукт |
| --- | --- | --- | --- | --- |
| 1–2 | Разбор датасета, базовая модель, заглушки `compute`/`score` | OpenAlex + arXiv, таблицы БД | Репозиторий, docker-compose, Ollama + Qwen, CLI на заглушках | Вопросы организаторам, черновик правил |
| 3–4 | Признаки, CatBoost | Патенты, новости, КиберЛенинка | `expand_query`, `extract_candidates` | Доверие к доменам, тестовые запросы |
| 5–6 | SHAP, порог, F1 ≥ 0,8 | `term_stats` | Конвейер целиком в консоли | Правила отсева, проверка выдачи |
| 7–8 | Общий модуль признаков для обоих этапов | Кэш, таймауты | Карточки с проверкой ссылок | Методология |
| 9 | Отчёт по метрикам | Правки | README, запуск с нуля | Прогон 5 тестовых запросов |
| 10 | Сдача | — | — | — |

После промежуточной: веб-интерфейс и страница-отчёт → качество выдачи и скорость → стенд, презентация (14 слайдов), 3+ репетиции демо.

## Главные риски

- Признаки датасета нельзя посчитать по открытым источникам → проверить в дни 1–2; держать «переносимую» модель.
- Локальная LLM медленная на ноутбуке → модель 4–8B, короткие промпты, подробный отчёт по клику.
- Код от Claude у разных людей не стыкуется → контракты в `schemas.py`, заглушки с первого дня, ежедневная сборка.
- LLM выдумывает ссылки → только RAG, проверка кодом, что каждая ссылка из собранных документов.

## Вопросы организаторам (ещё не заданы)

- Порог 75% или 80%, и какая метрика главная: accuracy или F1?
- Какие поля будут в датасете?
- Как проверяют скрытую часть: мы шлём предсказания или они запускают код?
- Дата финала и где разворачивать стенд?
- Будет ли интернет на демо, можно ли кэш?
- Можно ли облачный API как запасной?
- «gpt-5.6-luna» — правильное название?
- Будет ли заранее список тем для демо?
- **Главное:** файл «100 слабых технологических сигналов (сентябрь 2026)» — это датасет этапа 1? В ТЗ описан CSV/JSON о разных технологиях со скрытой разметкой, а в файле xlsx и только слабые сигналы. Какие столбцы будут в закрытом датасете и как проверяется точность: мы присылаем предсказания или вы запускаете наш код?

## Уточнения после перечитывания ТЗ (18.09.2026)

- Выданный xlsx — 100 технологий, **все** — слабые сигналы (6 областей по ~17: индустриальный ИИ, инфраструктура ИИ, роботы, финтех, edge, защита ИИ). Не совпадает с описанием датасета в ТЗ → вопрос организаторам выше.
- Пока используем его как положительный класс + наши отрицательные примеры (`src/model/training/negatives.csv`, 253 шт.: зрелые, массовые-но-растущие, хайп, шум) и как эталон для проверки этапа 2.
- Модель — CatBoost + SHAP на признаках из открытых источников; LLM (Qwen) не дообучаем: ТЗ требует объяснимости по признакам и запрещает выдачу «из знаний модели». Если в закрытом датасете будут текстовые описания — добавим текстовые признаки (гибрид).
- Первый эксперимент (`notebooks/01_openalex_probe.py`): только по динамике публикаций OpenAlex сигналы отделяются от не-сигналов с ROC-AUC ≈ 0.82–0.86 (0.92 без поправки на утечку через длину поисковой фразы).
- OpenAlex без ключа: лимит ~$0.10 в день, `group_by` стоит $0.0001 → ~1000 запросов в день.
- Из ТЗ, чего не было в плане: презентация ≤ 15 слайдов; техдокументация со схемами (PlantUML/Draw.io); к финалу — развёрнутый стенд для жюри.

## Что уже есть вне репозитория

- План решения — документ в Claude («План решения кейса «Слабые сигналы»») и его PDF для команды.
- Инструкция по раздаче ролей — `КАК_РАЗДАТЬ_РОЛИ.md` (в архиве рядом с репозиторием).

## Следующие шаги artiom

1. Создать репозиторий на GitHub, залить каркас, пригласить ребят, раздать стартовые сообщения.
2. Вписать имена в таблицу ролей в `CLAUDE.md`.
3. Заглушки `src/features/compute.py` и `src/model/score.py` с правильными сигнатурами.
4. Разбор датасета, как только его выдадут.
